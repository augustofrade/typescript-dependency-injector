import { inject } from "./common";
import { DI } from "./core";
import { InjectableSingleton, InjectableTransient } from "./core/decorators";

export { DI, inject, InjectableSingleton, InjectableTransient };
